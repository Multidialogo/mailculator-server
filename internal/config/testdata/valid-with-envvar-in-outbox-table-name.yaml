mysql:
  host: "${TEST_ENV_VAR}"
  port: 3306
  user: "root"
  password: "password"
  database: "mailculator"
  tls: "false"

payload-storage:
  path: "/efs/json"

outbox:
  stale-emails-threshold-minutes: 30

server:
  port: 8080
