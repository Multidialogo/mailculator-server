aws:
  base-endpoint: "endpoint.local"

attachments:
  base-path: "/efs/attachments"

payload-storage:
  path: "/efs/json"

outbox:
  table-name: "${TEST_ENV_VAR}"
  stale-emails-threshold-minutes: 30

server:
  port: 8080
