mysql:
  host: "localhost"
  port: 3306
  user: "root"
  password: "password"
  database: "mailculator"

payload-storage:
  path: "/efs/json"

outbox:
  stale-emails-threshold-minutes: 30

server:
  port: 8080
