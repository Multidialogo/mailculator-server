attachments:
  base-path: "/efs/attachments"

eml-storage:
  path: "/efs/eml"

payload-storage:
  path: "/efs/payloads"

outbox:
  table-name: "Outbox"
  stale-emails-threshold-minutes: 30

server:
  port: 8080
