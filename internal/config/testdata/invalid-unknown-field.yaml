mysql:
  host: "localhost"
  port: 3306
  user: "root"
  password: "password"
  database: "mailculator"
  unknown-field: "this-should-fail"

outbox:
  stale-emails-threshold-minutes: 30

server:
  port: 8080
