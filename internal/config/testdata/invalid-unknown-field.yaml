aws:
  region: "dummy-region-with-quotation-marks"
  base-endpoint: dummy-base-endpoint

attachments:
  base-path: "/efs/attachments"

eml-storage:
  path: "/efs/eml"

outbox:
  table-name: "Outbox"

server:
  port: 8080
